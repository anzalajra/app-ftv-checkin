# Application Check-in

## Installation
To install the required dependencies, make sure you have Python installed. You can install PyQt5 by running the following command:

```bash
python -m pip install pyqt5
```

## Running the Application

To run the application, use the following command:

```bash
python main.py
```

Ensure that you are in the project directory before running the command.